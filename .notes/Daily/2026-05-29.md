# 2026-05-29

## Focus

- Make Obsidian useful for MerHouse without turning private notes into a second source of truth.

## Roadmap Touchpoints

- [[docs/architecture/roadmap#Roadmap QC Rules|Roadmap QC Rules]]
- [[docs/architecture/roadmap#Change Quality Rule|Change Quality Rule]]
- [[docs/development/knowledge-system|Knowledge system]]
- [[AGENTS#Markdown And Obsidian|Agent markdown guidance]]

## Notes

- Obsidian vault root: `D:\MerHouse`
- Private notes folder: `.notes/`
- Obsidian settings folder: `.obsidian/`
- Obsidian settings are ignored by Git; private notes are allowed to be tracked in this private repository.
- V13 foundation note: [[.notes/Features/V13 Notifications Foundation|V13 Notifications Foundation]]

## Proof

- Publication-facing guardrail: `.gitignore` keeps Obsidian local settings ignored while allowing private markdown notes to be tracked outside the future app publication boundary.
- Markdown guardrail: `scripts/quality/markdown-check.ps1` validates markdown and Obsidian links.
- Run: `.\scripts\quality\markdown-check.ps1`
- Run: `.\scripts\quality\public-readiness.ps1 -SkipCompose`
- Run: `.\scripts\quality\check.ps1 -IncludeE2E -SkipCompose`
- Result: backend tests, frontend lint/build/Vitest, Playwright E2E, markdown link proof, and publication-boundary proof passed against a temporary seeded local stack.
- Run: `.\scripts\quality\frontend-check.ps1 -SkipInstall -IncludeE2E`
- Result: live-view cleanup passed frontend lint, build, Vitest, and Playwright E2E against the running seeded stack.
- Run: `cd backend; .\mvnw -q "-Dtest=NotificationServiceTest,AuthRecoveryServiceTest,AccessRequestServiceTest" test`
- Result: focused V13 backend notification, recovery, and access-request lifecycle tests passed.
- Run: `cd frontend; npm test -- --run src/pages/NotificationCenterPage.test.tsx src/components/AppLayout.test.tsx src/api/client.test.ts`
- Result: focused V13 frontend notification route, role navigation, and API client tests passed.
- Run: `.\scripts\local\start.ps1 -ResetDatabase`
- Result: clean Docker stack built and started with frontend, backend, and Postgres.
- Run: `.\scripts\local\seed-demo.ps1 -CreateReviewAccounts -SuppressCredentialOutput`
- Result: seeded review accounts and operational demo data without printing review credentials.
- Run: `.\scripts\quality\frontend-check.ps1 -SkipInstall -IncludeE2E`
- Result: frontend lint, build, full Vitest, and 12 Playwright E2E tests passed, including the full route tour with `/notifications`.
- Live browser proof: `reports/notification-live-check.png` captured the Docker frontend at `/notifications`; `reports/` remains ignored and outside the future public app boundary.
- V13 polling slice: added notification summary endpoint, app-shell unread alert badge, and notification page polling refresh.
- Run: `cd backend; .\mvnw -q "-Dtest=NotificationServiceTest" test`
- Result: focused backend summary and notification service tests passed.
- Run: `cd frontend; npm test -- --run src/pages/NotificationCenterPage.test.tsx src/components/AppLayout.test.tsx src/api/client.test.ts`
- Result: focused frontend polling, navigation badge, and API client tests passed.
- Run: `cd frontend; npm run lint`
- Result: lint passed after polling refresh changes.
- Run: `cd backend; .\mvnw -q test`
- Result: full backend tests passed; Flyway validated and migrated 16 migrations from an empty Testcontainers Postgres database.
- Run: `cd frontend; npm run build`
- Result: frontend production build passed.
- Run: `cd frontend; npm test -- --run`
- Result: full Vitest suite passed with 61 tests.
- Run: `.\scripts\quality\markdown-check.ps1`
- Result: markdown and Obsidian links passed.
- Run: `.\scripts\quality\public-readiness.ps1 -SkipCompose`
- Result: publication boundary, CI naming, sensitive-file, and sensitive-pattern scans passed.
- Run: `.\scripts\local\start.ps1 -ResetDatabase`
- Result: rebuilt and restarted clean Docker stack with the notification summary endpoint included.
- Run: `.\scripts\local\seed-demo.ps1 -CreateReviewAccounts -SuppressCredentialOutput`
- Result: seeded review accounts and operational data without printing review credentials.
- Run: `.\scripts\quality\frontend-check.ps1 -SkipInstall -IncludeE2E`
- Result: frontend lint, build, full Vitest, and 12 Playwright E2E tests passed against the rebuilt seeded stack.
- Live browser proof: requested a prototype-local password reset for `admin@merhouse.local`, logged in through the Docker frontend, and verified the `Alerts` navigation badge exposed `1 unread alerts` with no horizontal overflow. Browser screenshot capture timed out, so the retained proof is DOM/Playwright output rather than a new screenshot.
- V13 visibility slice: added controller tests proving notification endpoints scope by authenticated user id, frontend tests proving every role gets only its own notification surface, and Playwright proof that a password-reset delivery for one admin account is absent for a support-admin account.
- Run: `cd backend; .\mvnw -q "-Dtest=NotificationServiceTest,NotificationControllerTest" test`
- Result: focused notification service and controller visibility tests passed.
- Run: `cd frontend; npm test -- --run src/pages/NotificationCenterPage.test.tsx src/components/AppLayout.test.tsx src/api/client.test.ts`
- Result: focused frontend notification visibility and API tests passed with 17 tests.
- Run: `cd frontend; npm run lint`
- Result: lint passed after visibility proof changes.
- Run: `cd backend; .\mvnw -q test`
- Result: full backend tests passed.
- Run: `cd frontend; npm run build`
- Result: frontend build passed.
- Run: `cd frontend; npm test -- --run`
- Result: full Vitest suite passed with 68 tests.
- Run: `.\scripts\quality\public-readiness.ps1 -SkipCompose`
- Result: publication boundary, CI naming, sensitive-file, and sensitive-pattern scans passed.
- Run: `.\scripts\quality\frontend-check.ps1 -SkipInstall -IncludeE2E`
- Result: frontend lint, build, full Vitest, and 13 Playwright E2E tests passed, including notification recipient scoping.
- V13 production-shaped lifecycle slice: added explicit delivery stage and provider status fields so account lifecycle records show local recorded / provider not configured rather than implying provider delivery.
- Run: `cd backend; .\mvnw -q "-Dtest=NotificationServiceTest,NotificationControllerTest,AuthRecoveryServiceTest,AccessRequestServiceTest" test`
- Result: focused notification/account-lifecycle tests passed with production-shaped delivery state assertions.
- Run: `cd frontend; npm test -- --run src/pages/NotificationCenterPage.test.tsx src/components/AppLayout.test.tsx src/api/client.test.ts`
- Result: focused frontend tests passed with visible local/provider delivery state.
- Run: `cd frontend; npm run lint`
- Result: lint passed after lifecycle state UI changes.
- Run: `cd backend; .\mvnw -q test`
- Result: full backend tests passed; Flyway validated and migrated 16 migrations from an empty Testcontainers Postgres database after the delivery-stage/provider-status migration update.
- Run: `cd frontend; npm run build`
- Result: frontend production build passed after lifecycle state UI changes.
- Run: `cd frontend; npm test -- --run`
- Result: full Vitest suite passed with 68 tests.
- Run: `.\scripts\local\start.ps1 -ResetDatabase`
- Result: rebuilt the Docker frontend/backend/Postgres stack from a clean database with the production-shaped notification delivery fields.
- Run: `.\scripts\local\seed-demo.ps1 -CreateReviewAccounts -SuppressCredentialOutput`
- Result: seeded review accounts and operational demo data without printing review credentials.
- Run: `.\scripts\quality\frontend-check.ps1 -SkipInstall -IncludeE2E`
- Result: frontend lint, build, full Vitest, and 13 Playwright E2E tests passed against the rebuilt seeded stack, including notification recipient scoping.
- Live browser proof: requested a prototype-local password reset for `admin@merhouse.local`, opened the Docker frontend at `/notifications`, and verified the visible delivery card showed `Password reset prepared`, `Local recorded`, `Provider not configured`, and `Prototype-local`.
- Run: `.\scripts\quality\markdown-check.ps1`
- Result: markdown and Obsidian links passed after recording the lifecycle proof.
- Run: `.\scripts\quality\public-readiness.ps1 -SkipCompose`
- Result: publication boundary, CI naming, sensitive-file, and sensitive-pattern scans passed after the lifecycle proof.
